# Next Steps

## Immediate next implementation goals

1. Replace `spawnSync` with a persistent PTY-based session runtime.
2. Introduce a real Electron shell with a renderer UI.
3. Add a session timeline UI backed by normalized events.
4. Add transcript persistence and transcript browsing UX.
5. Add first-run setup for selecting Codex CLI and testing voice preferences.
6. Expand CI from structure checks to real build and validation steps.
